# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(profundimetroTaskLib)
ADD_LIBRARY(${PROFUNDIMETRO_TASKLIB_NAME} SHARED 
    ${PROFUNDIMETRO_TASKLIB_SOURCES})
add_dependencies(${PROFUNDIMETRO_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${PROFUNDIMETRO_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${PROFUNDIMETRO_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${PROFUNDIMETRO_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${PROFUNDIMETRO_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${PROFUNDIMETRO_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${PROFUNDIMETRO_TASKLIB_HEADERS}
    DESTINATION include/orocos/profundimetro)

